# Media

Static media served at `/media/...`.

- `images/day-1.jpg` … `day-21.jpg|png` — hero image per day (see `content/media-map.json` for the original filenames from the old site's `public/uploads/2016/10/`).
- `audio/exercise_audio_{6,9,11,14,21}.mp3` — guided meditation audio for those days (from the old site's `public/uploads/mp3/`).
- `audio/audio_play_1..21.mp3` — narrated version of each day (optional, not used yet).

Files are not in git yet; they are copied in from the Dropbox archive.
